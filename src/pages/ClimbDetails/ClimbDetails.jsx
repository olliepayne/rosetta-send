const ClimbDetails = () => {
 // pass api from app, call backend using useParams hook
 // return the corresponding climb, and display
 // if we are the owner of the climb, add the ability to update and delete

 return (
  <div>
   
  </div>
 )
}

export default ClimbDetails